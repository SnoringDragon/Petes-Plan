const courseModel = require('../models/courseModel');
const usercourseModel = require('../models/userCourseModel');

//create dummy data

//input - userCourse list, gather quality points for each
const input = [];

//map each userCourse to its course, gather credit hours for each

// dividing your quality points by your GPA hours
function calculateCumulativeGPA(gpas) {
    GPA hours 
}